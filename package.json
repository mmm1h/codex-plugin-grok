{
  "name": "@mmm1h/codex-plugin-grok",
  "version": "1.1.3-grok",
  "private": true,
  "type": "module",
  "description": "Use Codex from Grok Build to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "sync-installed": "node scripts/sync-installed-plugin.mjs",
    "prebuild": "node scripts/generate-app-server-types.mjs",
    "build": "tsc -p tsconfig.app-server.json",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  }
}
